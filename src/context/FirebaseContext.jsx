import { createContext } from "react";

/**
 * Placeholder context for future Firebase upgrade.
 * Keep app logic storage-agnostic so this can replace local storage later.
 */
export const FirebaseContext = createContext(null);
